{
  "name": "devframes",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=26.1.0"
  },
  "bin": {
    "devframes": "./update.mjs"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  }
}
